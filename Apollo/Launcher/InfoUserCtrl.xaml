<!--
//! Copyright(c) 2022 Frontier Development Plc
//
//! Product InfoUserCtrl
//
//! Author:     Alan MacAree
//! Created:    23 Dec 2022
-->

    <UserControl x:Class="Launcher.InfoUserCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Initialized="OnInitialized"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Bottom left corner text -->
        <StackPanel VerticalAlignment="Bottom" >

            <!-- Server Status -->
            <Label  x:Name="PART_ServerStatus"
                    Style="{StaticResource DefactSizeTextLabel}"
                    Foreground="Gray" 
                    Padding="0" 
                    Margin="0"
                    HorizontalAlignment="Left">
            </Label>

            <!-- Launcher Version -->
            <Label x:Name="PART_LauncherVersion"
                   Style="{StaticResource DefactSizeTextLabel}"
                   Foreground="Gray" 
                   Padding="0"
                   Margin="0"
                   HorizontalAlignment="Left">
            </Label>

            <!-- Product Name -->
            <Label x:Name="PART_ProjectName"
                   Style="{StaticResource DefactSizeTextLabel}"
                   Foreground="Gray" 
                   Padding="0" 
                   Margin="0"
                   HorizontalAlignment="Left">
            </Label>
            
        </StackPanel>
    </Grid>
        
</UserControl>
